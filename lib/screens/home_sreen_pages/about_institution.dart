import 'package:flutter/material.dart';
import 'package:sit_app/themes/themes.dart';

class AboutInstitution extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(
      child: Text(
        'AboutInstitution',
        style: MyTheme.h1(),
      ),
    ),);
  }
}
